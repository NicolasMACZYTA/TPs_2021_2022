#include "file_priorites_min.h"

/*sommet_t* extraire_min(file_t* f){
    int i;
    sommet_t* min = &f->stockage[f->tete];

    for ( i = 0; i < f->size; i++)
    {
        if(&f->stockage[i]){
            min = &f->stockage[i];
        }
    }
    return min;
}*/ 